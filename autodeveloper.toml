prompt = "Your task is to build THE best next.js app. Write tests and make sure they pass."
retry_prompt = "The PR was NOT created yet. Check git status, create a branch, commit, push, and run `gh pr create --fill`. Do NOT give up."
model = "opencode/kimi-k2.5-free"
